// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See LICENSE in the project root for license information.
// Code generated by Microsoft (R) AutoRest Code Generator. DO NOT EDIT.
// Changes may cause incorrect behavior and will be lost if the code is regenerated.
// SPDX-License-Identifier: MIT

package fake

import (
	"context"
	"errors"
	"fmt"
	"net/http"
	"net/url"
	"regexp"
	"strconv"
	"strings"

	azfake "github.com/Azure/azure-sdk-for-go/sdk/azcore/fake"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/fake/server"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/runtime"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/to"

	"github.com/microsoft/fabric-sdk-go/fabric/plan"
)

// ItemsServer is a fake server for instances of the plan.ItemsClient type.
type ItemsServer struct {
	// BeginCreatePlan is the fake for method ItemsClient.BeginCreatePlan
	// HTTP status codes to indicate success: http.StatusOK, http.StatusCreated, http.StatusAccepted
	BeginCreatePlan func(ctx context.Context, workspaceID string, createPlanRequest plan.CreatePlanRequest, options *plan.ItemsClientBeginCreatePlanOptions) (resp azfake.PollerResponder[plan.ItemsClientCreatePlanResponse], errResp azfake.ErrorResponder)

	// DeletePlan is the fake for method ItemsClient.DeletePlan
	// HTTP status codes to indicate success: http.StatusOK
	DeletePlan func(ctx context.Context, workspaceID string, planID string, options *plan.ItemsClientDeletePlanOptions) (resp azfake.Responder[plan.ItemsClientDeletePlanResponse], errResp azfake.ErrorResponder)

	// GetPlan is the fake for method ItemsClient.GetPlan
	// HTTP status codes to indicate success: http.StatusOK
	GetPlan func(ctx context.Context, workspaceID string, planID string, options *plan.ItemsClientGetPlanOptions) (resp azfake.Responder[plan.ItemsClientGetPlanResponse], errResp azfake.ErrorResponder)

	// BeginGetPlanDefinition is the fake for method ItemsClient.BeginGetPlanDefinition
	// HTTP status codes to indicate success: http.StatusOK, http.StatusAccepted
	BeginGetPlanDefinition func(ctx context.Context, workspaceID string, planID string, options *plan.ItemsClientBeginGetPlanDefinitionOptions) (resp azfake.PollerResponder[plan.ItemsClientGetPlanDefinitionResponse], errResp azfake.ErrorResponder)

	// NewListPlansPager is the fake for method ItemsClient.NewListPlansPager
	// HTTP status codes to indicate success: http.StatusOK
	NewListPlansPager func(workspaceID string, options *plan.ItemsClientListPlansOptions) (resp azfake.PagerResponder[plan.ItemsClientListPlansResponse])

	// UpdatePlan is the fake for method ItemsClient.UpdatePlan
	// HTTP status codes to indicate success: http.StatusOK
	UpdatePlan func(ctx context.Context, workspaceID string, planID string, updatePlanRequest plan.UpdatePlanRequest, options *plan.ItemsClientUpdatePlanOptions) (resp azfake.Responder[plan.ItemsClientUpdatePlanResponse], errResp azfake.ErrorResponder)

	// BeginUpdatePlanDefinition is the fake for method ItemsClient.BeginUpdatePlanDefinition
	// HTTP status codes to indicate success: http.StatusOK, http.StatusAccepted, http.StatusNoContent
	BeginUpdatePlanDefinition func(ctx context.Context, workspaceID string, planID string, updatePlanDefinitionRequest plan.UpdatePlanDefinitionRequest, options *plan.ItemsClientBeginUpdatePlanDefinitionOptions) (resp azfake.PollerResponder[plan.ItemsClientUpdatePlanDefinitionResponse], errResp azfake.ErrorResponder)
}

// NewItemsServerTransport creates a new instance of ItemsServerTransport with the provided implementation.
// The returned ItemsServerTransport instance is connected to an instance of plan.ItemsClient via the
// azcore.ClientOptions.Transporter field in the client's constructor parameters.
func NewItemsServerTransport(srv *ItemsServer) *ItemsServerTransport {
	return &ItemsServerTransport{
		srv:                       srv,
		beginCreatePlan:           newTracker[azfake.PollerResponder[plan.ItemsClientCreatePlanResponse]](),
		beginGetPlanDefinition:    newTracker[azfake.PollerResponder[plan.ItemsClientGetPlanDefinitionResponse]](),
		newListPlansPager:         newTracker[azfake.PagerResponder[plan.ItemsClientListPlansResponse]](),
		beginUpdatePlanDefinition: newTracker[azfake.PollerResponder[plan.ItemsClientUpdatePlanDefinitionResponse]](),
	}
}

// ItemsServerTransport connects instances of plan.ItemsClient to instances of ItemsServer.
// Don't use this type directly, use NewItemsServerTransport instead.
type ItemsServerTransport struct {
	srv                       *ItemsServer
	beginCreatePlan           *tracker[azfake.PollerResponder[plan.ItemsClientCreatePlanResponse]]
	beginGetPlanDefinition    *tracker[azfake.PollerResponder[plan.ItemsClientGetPlanDefinitionResponse]]
	newListPlansPager         *tracker[azfake.PagerResponder[plan.ItemsClientListPlansResponse]]
	beginUpdatePlanDefinition *tracker[azfake.PollerResponder[plan.ItemsClientUpdatePlanDefinitionResponse]]
}

// Do implements the policy.Transporter interface for ItemsServerTransport.
func (i *ItemsServerTransport) Do(req *http.Request) (*http.Response, error) {
	rawMethod := req.Context().Value(runtime.CtxAPINameKey{})
	method, ok := rawMethod.(string)
	if !ok {
		return nil, nonRetriableError{errors.New("unable to dispatch request, missing value for CtxAPINameKey")}
	}

	parts := strings.Split(method, ".")
	method = parts[1] + "." + parts[2]
	return i.dispatchToMethodFake(req, method)
}

func (i *ItemsServerTransport) dispatchToMethodFake(req *http.Request, method string) (*http.Response, error) {
	resultChan := make(chan result)
	defer close(resultChan)

	go func() {
		var intercepted bool
		var res result
		if itemsServerTransportInterceptor != nil {
			res.resp, res.err, intercepted = itemsServerTransportInterceptor.Do(req)
		}
		if !intercepted {
			switch method {
			case "ItemsClient.BeginCreatePlan":
				res.resp, res.err = i.dispatchBeginCreatePlan(req)
			case "ItemsClient.DeletePlan":
				res.resp, res.err = i.dispatchDeletePlan(req)
			case "ItemsClient.GetPlan":
				res.resp, res.err = i.dispatchGetPlan(req)
			case "ItemsClient.BeginGetPlanDefinition":
				res.resp, res.err = i.dispatchBeginGetPlanDefinition(req)
			case "ItemsClient.NewListPlansPager":
				res.resp, res.err = i.dispatchNewListPlansPager(req)
			case "ItemsClient.UpdatePlan":
				res.resp, res.err = i.dispatchUpdatePlan(req)
			case "ItemsClient.BeginUpdatePlanDefinition":
				res.resp, res.err = i.dispatchBeginUpdatePlanDefinition(req)
			default:
				res.err = fmt.Errorf("unhandled API %s", method)
			}

		}
		select {
		case resultChan <- res:
		case <-req.Context().Done():
		}
	}()

	select {
	case <-req.Context().Done():
		return nil, req.Context().Err()
	case res := <-resultChan:
		return res.resp, res.err
	}
}

func (i *ItemsServerTransport) dispatchBeginCreatePlan(req *http.Request) (*http.Response, error) {
	if i.srv.BeginCreatePlan == nil {
		return nil, &nonRetriableError{errors.New("fake for method BeginCreatePlan not implemented")}
	}
	beginCreatePlan := i.beginCreatePlan.get(req)
	if beginCreatePlan == nil {
		const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/plans`
		regex := regexp.MustCompile(regexStr)
		matches := regex.FindStringSubmatch(req.URL.EscapedPath())
		if len(matches) < 2 {
			return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
		}
		body, err := server.UnmarshalRequestAsJSON[plan.CreatePlanRequest](req)
		if err != nil {
			return nil, err
		}
		workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
		if err != nil {
			return nil, err
		}
		respr, errRespr := i.srv.BeginCreatePlan(req.Context(), workspaceIDParam, body, nil)
		if respErr := server.GetError(errRespr, req); respErr != nil {
			return nil, respErr
		}
		beginCreatePlan = &respr
		i.beginCreatePlan.add(req, beginCreatePlan)
	}

	resp, err := server.PollerResponderNext(beginCreatePlan, req)
	if err != nil {
		return nil, err
	}

	if !contains([]int{http.StatusOK, http.StatusCreated, http.StatusAccepted}, resp.StatusCode) {
		i.beginCreatePlan.remove(req)
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK, http.StatusCreated, http.StatusAccepted", resp.StatusCode)}
	}
	if !server.PollerResponderMore(beginCreatePlan) {
		i.beginCreatePlan.remove(req)
	}

	return resp, nil
}

func (i *ItemsServerTransport) dispatchDeletePlan(req *http.Request) (*http.Response, error) {
	if i.srv.DeletePlan == nil {
		return nil, &nonRetriableError{errors.New("fake for method DeletePlan not implemented")}
	}
	const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/plans/(?P<planId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)`
	regex := regexp.MustCompile(regexStr)
	matches := regex.FindStringSubmatch(req.URL.EscapedPath())
	if len(matches) < 3 {
		return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
	}
	workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
	if err != nil {
		return nil, err
	}
	planIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("planId")])
	if err != nil {
		return nil, err
	}
	respr, errRespr := i.srv.DeletePlan(req.Context(), workspaceIDParam, planIDParam, nil)
	if respErr := server.GetError(errRespr, req); respErr != nil {
		return nil, respErr
	}
	respContent := server.GetResponseContent(respr)
	if !contains([]int{http.StatusOK}, respContent.HTTPStatus) {
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", respContent.HTTPStatus)}
	}
	resp, err := server.NewResponse(respContent, req, nil)
	if err != nil {
		return nil, err
	}
	return resp, nil
}

func (i *ItemsServerTransport) dispatchGetPlan(req *http.Request) (*http.Response, error) {
	if i.srv.GetPlan == nil {
		return nil, &nonRetriableError{errors.New("fake for method GetPlan not implemented")}
	}
	const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/plans/(?P<planId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)`
	regex := regexp.MustCompile(regexStr)
	matches := regex.FindStringSubmatch(req.URL.EscapedPath())
	if len(matches) < 3 {
		return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
	}
	workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
	if err != nil {
		return nil, err
	}
	planIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("planId")])
	if err != nil {
		return nil, err
	}
	respr, errRespr := i.srv.GetPlan(req.Context(), workspaceIDParam, planIDParam, nil)
	if respErr := server.GetError(errRespr, req); respErr != nil {
		return nil, respErr
	}
	respContent := server.GetResponseContent(respr)
	if !contains([]int{http.StatusOK}, respContent.HTTPStatus) {
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", respContent.HTTPStatus)}
	}
	resp, err := server.MarshalResponseAsJSON(respContent, server.GetResponse(respr).Plan, req)
	if err != nil {
		return nil, err
	}
	return resp, nil
}

func (i *ItemsServerTransport) dispatchBeginGetPlanDefinition(req *http.Request) (*http.Response, error) {
	if i.srv.BeginGetPlanDefinition == nil {
		return nil, &nonRetriableError{errors.New("fake for method BeginGetPlanDefinition not implemented")}
	}
	beginGetPlanDefinition := i.beginGetPlanDefinition.get(req)
	if beginGetPlanDefinition == nil {
		const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/plans/(?P<planId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/getDefinition`
		regex := regexp.MustCompile(regexStr)
		matches := regex.FindStringSubmatch(req.URL.EscapedPath())
		if len(matches) < 3 {
			return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
		}
		qp := req.URL.Query()
		workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
		if err != nil {
			return nil, err
		}
		planIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("planId")])
		if err != nil {
			return nil, err
		}
		formatUnescaped, err := url.QueryUnescape(qp.Get("format"))
		if err != nil {
			return nil, err
		}
		formatParam := getOptional(formatUnescaped)
		var options *plan.ItemsClientBeginGetPlanDefinitionOptions
		if formatParam != nil {
			options = &plan.ItemsClientBeginGetPlanDefinitionOptions{
				Format: formatParam,
			}
		}
		respr, errRespr := i.srv.BeginGetPlanDefinition(req.Context(), workspaceIDParam, planIDParam, options)
		if respErr := server.GetError(errRespr, req); respErr != nil {
			return nil, respErr
		}
		beginGetPlanDefinition = &respr
		i.beginGetPlanDefinition.add(req, beginGetPlanDefinition)
	}

	resp, err := server.PollerResponderNext(beginGetPlanDefinition, req)
	if err != nil {
		return nil, err
	}

	if !contains([]int{http.StatusOK, http.StatusAccepted}, resp.StatusCode) {
		i.beginGetPlanDefinition.remove(req)
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK, http.StatusAccepted", resp.StatusCode)}
	}
	if !server.PollerResponderMore(beginGetPlanDefinition) {
		i.beginGetPlanDefinition.remove(req)
	}

	return resp, nil
}

func (i *ItemsServerTransport) dispatchNewListPlansPager(req *http.Request) (*http.Response, error) {
	if i.srv.NewListPlansPager == nil {
		return nil, &nonRetriableError{errors.New("fake for method NewListPlansPager not implemented")}
	}
	newListPlansPager := i.newListPlansPager.get(req)
	if newListPlansPager == nil {
		const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/plans`
		regex := regexp.MustCompile(regexStr)
		matches := regex.FindStringSubmatch(req.URL.EscapedPath())
		if len(matches) < 2 {
			return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
		}
		qp := req.URL.Query()
		workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
		if err != nil {
			return nil, err
		}
		recursiveUnescaped, err := url.QueryUnescape(qp.Get("recursive"))
		if err != nil {
			return nil, err
		}
		recursiveParam, err := parseOptional(recursiveUnescaped, strconv.ParseBool)
		if err != nil {
			return nil, err
		}
		rootFolderIDUnescaped, err := url.QueryUnescape(qp.Get("rootFolderId"))
		if err != nil {
			return nil, err
		}
		rootFolderIDParam := getOptional(rootFolderIDUnescaped)
		continuationTokenUnescaped, err := url.QueryUnescape(qp.Get("continuationToken"))
		if err != nil {
			return nil, err
		}
		continuationTokenParam := getOptional(continuationTokenUnescaped)
		var options *plan.ItemsClientListPlansOptions
		if recursiveParam != nil || rootFolderIDParam != nil || continuationTokenParam != nil {
			options = &plan.ItemsClientListPlansOptions{
				Recursive:         recursiveParam,
				RootFolderID:      rootFolderIDParam,
				ContinuationToken: continuationTokenParam,
			}
		}
		resp := i.srv.NewListPlansPager(workspaceIDParam, options)
		newListPlansPager = &resp
		i.newListPlansPager.add(req, newListPlansPager)
		server.PagerResponderInjectNextLinks(newListPlansPager, req, func(page *plan.ItemsClientListPlansResponse, createLink func() string) {
			page.ContinuationURI = to.Ptr(createLink())
		})
	}
	resp, err := server.PagerResponderNext(newListPlansPager, req)
	if err != nil {
		return nil, err
	}
	if !contains([]int{http.StatusOK}, resp.StatusCode) {
		i.newListPlansPager.remove(req)
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", resp.StatusCode)}
	}
	if !server.PagerResponderMore(newListPlansPager) {
		i.newListPlansPager.remove(req)
	}
	return resp, nil
}

func (i *ItemsServerTransport) dispatchUpdatePlan(req *http.Request) (*http.Response, error) {
	if i.srv.UpdatePlan == nil {
		return nil, &nonRetriableError{errors.New("fake for method UpdatePlan not implemented")}
	}
	const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/plans/(?P<planId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)`
	regex := regexp.MustCompile(regexStr)
	matches := regex.FindStringSubmatch(req.URL.EscapedPath())
	if len(matches) < 3 {
		return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
	}
	body, err := server.UnmarshalRequestAsJSON[plan.UpdatePlanRequest](req)
	if err != nil {
		return nil, err
	}
	workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
	if err != nil {
		return nil, err
	}
	planIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("planId")])
	if err != nil {
		return nil, err
	}
	respr, errRespr := i.srv.UpdatePlan(req.Context(), workspaceIDParam, planIDParam, body, nil)
	if respErr := server.GetError(errRespr, req); respErr != nil {
		return nil, respErr
	}
	respContent := server.GetResponseContent(respr)
	if !contains([]int{http.StatusOK}, respContent.HTTPStatus) {
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", respContent.HTTPStatus)}
	}
	resp, err := server.MarshalResponseAsJSON(respContent, server.GetResponse(respr).Plan, req)
	if err != nil {
		return nil, err
	}
	return resp, nil
}

func (i *ItemsServerTransport) dispatchBeginUpdatePlanDefinition(req *http.Request) (*http.Response, error) {
	if i.srv.BeginUpdatePlanDefinition == nil {
		return nil, &nonRetriableError{errors.New("fake for method BeginUpdatePlanDefinition not implemented")}
	}
	beginUpdatePlanDefinition := i.beginUpdatePlanDefinition.get(req)
	if beginUpdatePlanDefinition == nil {
		const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/plans/(?P<planId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/updateDefinition`
		regex := regexp.MustCompile(regexStr)
		matches := regex.FindStringSubmatch(req.URL.EscapedPath())
		if len(matches) < 3 {
			return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
		}
		body, err := server.UnmarshalRequestAsJSON[plan.UpdatePlanDefinitionRequest](req)
		if err != nil {
			return nil, err
		}
		workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
		if err != nil {
			return nil, err
		}
		planIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("planId")])
		if err != nil {
			return nil, err
		}
		respr, errRespr := i.srv.BeginUpdatePlanDefinition(req.Context(), workspaceIDParam, planIDParam, body, nil)
		if respErr := server.GetError(errRespr, req); respErr != nil {
			return nil, respErr
		}
		beginUpdatePlanDefinition = &respr
		i.beginUpdatePlanDefinition.add(req, beginUpdatePlanDefinition)
	}

	resp, err := server.PollerResponderNext(beginUpdatePlanDefinition, req)
	if err != nil {
		return nil, err
	}

	if !contains([]int{http.StatusOK, http.StatusAccepted, http.StatusNoContent}, resp.StatusCode) {
		i.beginUpdatePlanDefinition.remove(req)
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK, http.StatusAccepted, http.StatusNoContent", resp.StatusCode)}
	}
	if !server.PollerResponderMore(beginUpdatePlanDefinition) {
		i.beginUpdatePlanDefinition.remove(req)
	}

	return resp, nil
}

// set this to conditionally intercept incoming requests to ItemsServerTransport
var itemsServerTransportInterceptor interface {
	// Do returns true if the server transport should use the returned response/error
	Do(*http.Request) (*http.Response, error, bool)
}
