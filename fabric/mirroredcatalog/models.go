// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See LICENSE in the project root for license information.
// Code generated by Microsoft (R) AutoRest Code Generator. DO NOT EDIT.
// Changes may cause incorrect behavior and will be lost if the code is regenerated.
// SPDX-License-Identifier: MIT

package mirroredcatalog

import "time"

// CreateMirroredCatalogRequest - Create mirrored catalog request payload.
type CreateMirroredCatalogRequest struct {
	// REQUIRED; The mirrored catalog public definition.
	Definition *Definition

	// REQUIRED; The mirrored catalog display name. The display name must follow naming rules according to item type.
	DisplayName *string

	// The mirrored catalog description. Maximum length is 256 characters.
	Description *string

	// The folder ID. If not specified or null, the item is created with the workspace as its folder.
	FolderID *string
}

// Definition - Mirrored catalog public definition object. Refer to Mirrored Catalog definition [/rest/api/fabric/articles/item-management/definitions/mirrored-catalog-definition]
// article for more details.
type Definition struct {
	// REQUIRED; A list of definition parts.
	Parts []DefinitionPart

	// The format of the mirrored catalog definition.
	Format *string
}

// DefinitionPart - Mirrored catalog definition part object.
type DefinitionPart struct {
	// The mirrored catalog definition part path.
	Path *string

	// The mirrored catalog definition part payload.
	Payload *string

	// The payload type.
	PayloadType *PayloadType
}

// DefinitionResponse - Mirrored catalog public definition response.
type DefinitionResponse struct {
	// READ-ONLY; Mirrored catalog public definition object. Refer to Mirrored Catalog definition [/rest/api/fabric/articles/item-management/definitions/mirrored-catalog-definition]
	// article for more details.
	Definition *Definition
}

// EntireTenantPrincipal - Represents a tenant principal
type EntireTenantPrincipal struct {
	// REQUIRED; The principal's ID.
	ID *string

	// REQUIRED; The type of the principal. Additional principal types may be added over time.
	Type *PrincipalType

	// READ-ONLY; The principal's display name.
	DisplayName *string
}

// GetPrincipal implements the PrincipalClassification interface for type EntireTenantPrincipal.
func (e *EntireTenantPrincipal) GetPrincipal() *Principal {
	return &Principal{
		DisplayName: e.DisplayName,
		ID:          e.ID,
		Type:        e.Type,
	}
}

// ErrorRelatedResource - The error related resource details object.
type ErrorRelatedResource struct {
	// READ-ONLY; The resource ID that's involved in the error.
	ResourceID *string

	// READ-ONLY; The type of the resource that's involved in the error.
	ResourceType *string
}

// ErrorResponse - The error response.
type ErrorResponse struct {
	// READ-ONLY; A specific identifier that provides information about an error condition, allowing for standardized communication
	// between our service and its users.
	ErrorCode *string

	// READ-ONLY; A human readable representation of the error.
	Message *string

	// READ-ONLY; When true, the request can be retried. Use the Retry-After response header to determine the delay, if available.
	IsRetriable *bool

	// READ-ONLY; List of additional error details.
	MoreDetails []ErrorResponseDetails

	// READ-ONLY; The error related resource details.
	RelatedResource *ErrorRelatedResource

	// READ-ONLY; ID of the request associated with the error.
	RequestID *string
}

// ErrorResponseDetails - The error response details.
type ErrorResponseDetails struct {
	// READ-ONLY; A specific identifier that provides information about an error condition, allowing for standardized communication
	// between our service and its users.
	ErrorCode *string

	// READ-ONLY; A human readable representation of the error.
	Message *string

	// READ-ONLY; The error related resource details.
	RelatedResource *ErrorRelatedResource
}

// GroupPrincipal - Represents a security group.
type GroupPrincipal struct {
	// REQUIRED; The principal's ID.
	ID *string

	// REQUIRED; The type of the principal. Additional principal types may be added over time.
	Type *PrincipalType

	// Group specific details. Applicable when the principal type is Group.
	GroupDetails *GroupPrincipalGroupDetails

	// READ-ONLY; The principal's display name.
	DisplayName *string
}

// GetPrincipal implements the PrincipalClassification interface for type GroupPrincipal.
func (g *GroupPrincipal) GetPrincipal() *Principal {
	return &Principal{
		DisplayName: g.DisplayName,
		ID:          g.ID,
		Type:        g.Type,
	}
}

// GroupPrincipalGroupDetails - Group specific details. Applicable when the principal type is Group.
type GroupPrincipalGroupDetails struct {
	// The type of the group. Additional group types may be added over time.
	GroupType *GroupType
}

// ItemTag - Represents a tag applied on an item.
type ItemTag struct {
	// REQUIRED; The name of the tag.
	DisplayName *string

	// REQUIRED; The tag ID.
	ID *string
}

// MirroredCatalog - A mirrored catalog item.
type MirroredCatalog struct {
	// REQUIRED; The item type.
	Type *ItemType

	// The item description.
	Description *string

	// The item display name.
	DisplayName *string

	// READ-ONLY; The item's default identity.
	DefaultIdentity PrincipalClassification

	// READ-ONLY; The folder ID.
	FolderID *string

	// READ-ONLY; The item ID.
	ID *string

	// READ-ONLY; The mirrored catalog properties.
	Properties *Properties

	// READ-ONLY; The item sensitivity label.
	SensitivityLabel *SensitivityLabel

	// READ-ONLY; List of applied tags.
	Tags []ItemTag

	// READ-ONLY; The workspace ID.
	WorkspaceID *string
}

// MirroredCatalogs - A list of mirrored catalogs.
type MirroredCatalogs struct {
	// READ-ONLY; A list of mirrored catalogs.
	Value []MirroredCatalog

	// The token for the next result set batch. If there are no more records, it's removed from the response.
	ContinuationToken *string

	// The URI of the next result set batch. If there are no more records, it's removed from the response.
	ContinuationURI *string
}

// MirroringStatusResponse - Response containing the overall mirroring status of the mirrored catalog.
type MirroringStatusResponse struct {
	// READ-ONLY; Current mirror status.
	Status *ItemMirroringState

	// READ-ONLY; Item-level error details. Set when the item status is Failed. Null when the item is healthy or running.
	Error *ErrorResponse

	// READ-ONLY; Last sync attempt timestamp. Null if no sync has been attempted yet. The timestamp is in UTC, using the YYYY-MM-DDTHH:mm:ssZ
	// format.
	LastSyncDateTime *time.Time
}

// Principal - Represents an identity or a Microsoft Entra group.
type Principal struct {
	// REQUIRED; The principal's ID.
	ID *string

	// REQUIRED; The type of the principal. Additional principal types may be added over time.
	Type *PrincipalType

	// READ-ONLY; The principal's display name.
	DisplayName *string
}

// GetPrincipal implements the PrincipalClassification interface for type Principal.
func (p *Principal) GetPrincipal() *Principal { return p }

// Properties - The mirrored catalog properties.
type Properties struct {
	// READ-ONLY; The connection ID used for the mirrored catalog.
	ConnectionID *string

	// READ-ONLY; OneLake path to the mirrored catalog tables directory.
	OneLakeTablesPath *string

	// READ-ONLY; An object containing the properties of the SQL endpoint.
	SQLEndpointProperties *SQLEndpointProperties

	// READ-ONLY; The namespace hierarchy path that scopes the mirroring. Must be a Selectable scope as returned by the List Scopes
	// API.
	Scope []string

	// READ-ONLY; The source type for the underlying connection.
	SourceType *string
}

// SQLEndpointProperties - An object containing the properties of the SQL endpoint.
type SQLEndpointProperties struct {
	// READ-ONLY; SQL endpoint provisioning status. Additional statuses may be added over time.
	ProvisioningStatus *ProvisioningStatus

	// READ-ONLY; SQL endpoint connection string.
	ConnectionString *string

	// READ-ONLY; SQL endpoint ID.
	ID *string
}

// Scope - A namespace scope from the source catalog.
type Scope struct {
	// READ-ONLY; Full namespace hierarchy as an array. For Selectable namespaces, this array can be passed as the scope when
	// creating a MirroredCatalog.
	FullyQualifiedScope []string

	// READ-ONLY; Indicates whether this namespace can be selected as a scope.
	IsSelectable *IsSelectable

	// READ-ONLY; The namespace name (leaf segment of the hierarchy). When listing from the root level, the top-level entry is
	// a virtual (root) node.
	Name *string

	// READ-ONLY; Nested child namespaces. Only populated when recursive=true.
	Children []Scope
}

// ScopesResponse - A list of scopes from the source catalog. When listing from the root level (no parent specified), the
// response contains a single virtual (root) entry with fullyQualifiedScope: [] as the top-level item
// in value, with all actual namespaces nested under its children array. When parent is specified, the child namespaces are
// returned directly in value without the (root) wrapper.
type ScopesResponse struct {
	// READ-ONLY; A list of namespace scopes.
	Value []Scope

	// The token for the next result set batch. If there are no more records, it's removed from the response.
	ContinuationToken *string

	// The URI of the next result set batch. If there are no more records, it's removed from the response.
	ContinuationURI *string
}

// SensitivityLabel - Represents a sensitivity label applied to an item.
type SensitivityLabel struct {
	// REQUIRED; The sensitivity label ID.
	ID *string
}

// ServicePrincipal - Represents a Microsoft Entra service principal.
type ServicePrincipal struct {
	// REQUIRED; The principal's ID.
	ID *string

	// REQUIRED; The type of the principal. Additional principal types may be added over time.
	Type *PrincipalType

	// READ-ONLY; The principal's display name.
	DisplayName *string

	// READ-ONLY; Service principal specific details. Applicable when the principal type is ServicePrincipal.
	ServicePrincipalDetails *ServicePrincipalDetails
}

// GetPrincipal implements the PrincipalClassification interface for type ServicePrincipal.
func (s *ServicePrincipal) GetPrincipal() *Principal {
	return &Principal{
		DisplayName: s.DisplayName,
		ID:          s.ID,
		Type:        s.Type,
	}
}

// ServicePrincipalDetails - Service principal specific details. Applicable when the principal type is ServicePrincipal.
type ServicePrincipalDetails struct {
	// READ-ONLY; The service principal's Microsoft Entra AppId.
	AADAppID *string
}

// ServicePrincipalProfilePrincipal - Represents a service principal profile.
type ServicePrincipalProfilePrincipal struct {
	// REQUIRED; The principal's ID.
	ID *string

	// REQUIRED; The type of the principal. Additional principal types may be added over time.
	Type *PrincipalType

	// Service principal profile details. Applicable when the principal type is ServicePrincipalProfile.
	ServicePrincipalProfileDetails *ServicePrincipalProfilePrincipalServicePrincipalProfileDetails

	// READ-ONLY; The principal's display name.
	DisplayName *string
}

// GetPrincipal implements the PrincipalClassification interface for type ServicePrincipalProfilePrincipal.
func (s *ServicePrincipalProfilePrincipal) GetPrincipal() *Principal {
	return &Principal{
		DisplayName: s.DisplayName,
		ID:          s.ID,
		Type:        s.Type,
	}
}

// ServicePrincipalProfilePrincipalServicePrincipalProfileDetails - Service principal profile details. Applicable when the
// principal type is ServicePrincipalProfile.
type ServicePrincipalProfilePrincipalServicePrincipalProfileDetails struct {
	// The service principal profile's parent principal.
	ParentPrincipal PrincipalClassification
}

// TableInfo - Information about a table in the source catalog.
type TableInfo struct {
	// READ-ONLY; The complete namespace hierarchy from scope to the leaf schema containing the table. Use this value in mountedTables[].source.typeProperties.fullyQualifiedScope.
	FullyQualifiedScope []string

	// READ-ONLY; The table name. Use this value in mountedTables[].source.typeProperties.tableName.
	Name *string
}

// TableMirroringStatus - The mirroring status for a single table.
type TableMirroringStatus struct {
	// READ-ONLY; The table name.
	Name *string

	// READ-ONLY; The schema (leaf namespace) name where the table resides.
	SchemaName *string

	// READ-ONLY; Table mirroring state.
	Status *TableMirroringState

	// READ-ONLY; Populated when an error occurs while mirroring this table.
	Error *ErrorResponse

	// READ-ONLY; Last sync attempt timestamp for this table. Null if the table was discovered but has not completed its first
	// sync cycle yet. The timestamp is in UTC, using the YYYY-MM-DDTHH:mm:ssZ format.
	LastSyncDateTime *time.Time

	// READ-ONLY; Last successful shortcut creation or update timestamp for this table. Null if the shortcut has never been successfully
	// created. The timestamp is in UTC, using the YYYY-MM-DDTHH:mm:ssZ format.
	LastUpdateDateTime *time.Time
}

// TablesMirroringStatusResponse - Response containing the per-table mirroring status for the mirrored catalog.
type TablesMirroringStatusResponse struct {
	// READ-ONLY; A list of per-table mirroring statuses.
	Value []TableMirroringStatus

	// The token for the next result set batch. If there are no more records, it's removed from the response.
	ContinuationToken *string

	// The URI of the next result set batch. If there are no more records, it's removed from the response.
	ContinuationURI *string
}

// TablesResponse - A list of tables from the source catalog.
type TablesResponse struct {
	// READ-ONLY; A list of tables within the namespace scope.
	Value []TableInfo

	// The token for the next result set batch. If there are no more records, it's removed from the response.
	ContinuationToken *string

	// The URI of the next result set batch. If there are no more records, it's removed from the response.
	ContinuationURI *string
}

// UpdateMirroredCatalogDefinitionRequest - Update mirrored catalog public definition request payload.
type UpdateMirroredCatalogDefinitionRequest struct {
	// REQUIRED; Mirrored catalog public definition object. Refer to Mirrored Catalog definition [/rest/api/fabric/articles/item-management/definitions/mirrored-catalog-definition]
	// article for more details.
	Definition *Definition
}

// UpdateMirroredCatalogRequest - Update mirrored catalog request.
type UpdateMirroredCatalogRequest struct {
	// The mirrored catalog description. Maximum length is 256 characters.
	Description *string

	// The mirrored catalog display name. The display name must follow naming rules according to item type.
	DisplayName *string
}

// UserPrincipal - Represents a Microsoft Entra user principal.
type UserPrincipal struct {
	// REQUIRED; The principal's ID.
	ID *string

	// REQUIRED; The type of the principal. Additional principal types may be added over time.
	Type *PrincipalType

	// READ-ONLY; The principal's display name.
	DisplayName *string

	// READ-ONLY; User principal specific details. Applicable when the principal type is User.
	UserDetails *UserPrincipalUserDetails
}

// GetPrincipal implements the PrincipalClassification interface for type UserPrincipal.
func (u *UserPrincipal) GetPrincipal() *Principal {
	return &Principal{
		DisplayName: u.DisplayName,
		ID:          u.ID,
		Type:        u.Type,
	}
}

// UserPrincipalUserDetails - User principal specific details. Applicable when the principal type is User.
type UserPrincipalUserDetails struct {
	// READ-ONLY; The user principal name.
	UserPrincipalName *string
}
