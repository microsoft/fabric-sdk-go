// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See LICENSE in the project root for license information.
// Code generated by Microsoft (R) AutoRest Code Generator. DO NOT EDIT.
// Changes may cause incorrect behavior and will be lost if the code is regenerated.
// SPDX-License-Identifier: MIT

package fake

import (
	"context"
	"errors"
	"fmt"
	"net/http"
	"net/url"
	"regexp"
	"strings"

	azfake "github.com/Azure/azure-sdk-for-go/sdk/azcore/fake"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/fake/server"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/runtime"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/to"

	"github.com/microsoft/fabric-sdk-go/fabric/core"
)

// DomainsServer is a fake server for instances of the core.DomainsClient type.
type DomainsServer struct {
	// GetDomain is the fake for method DomainsClient.GetDomain
	// HTTP status codes to indicate success: http.StatusOK
	GetDomain func(ctx context.Context, domainID string, options *core.DomainsClientGetDomainOptions) (resp azfake.Responder[core.DomainsClientGetDomainResponse], errResp azfake.ErrorResponder)

	// NewListDomainsPager is the fake for method DomainsClient.NewListDomainsPager
	// HTTP status codes to indicate success: http.StatusOK
	NewListDomainsPager func(options *core.DomainsClientListDomainsOptions) (resp azfake.PagerResponder[core.DomainsClientListDomainsResponse])
}

// NewDomainsServerTransport creates a new instance of DomainsServerTransport with the provided implementation.
// The returned DomainsServerTransport instance is connected to an instance of core.DomainsClient via the
// azcore.ClientOptions.Transporter field in the client's constructor parameters.
func NewDomainsServerTransport(srv *DomainsServer) *DomainsServerTransport {
	return &DomainsServerTransport{
		srv:                 srv,
		newListDomainsPager: newTracker[azfake.PagerResponder[core.DomainsClientListDomainsResponse]](),
	}
}

// DomainsServerTransport connects instances of core.DomainsClient to instances of DomainsServer.
// Don't use this type directly, use NewDomainsServerTransport instead.
type DomainsServerTransport struct {
	srv                 *DomainsServer
	newListDomainsPager *tracker[azfake.PagerResponder[core.DomainsClientListDomainsResponse]]
}

// Do implements the policy.Transporter interface for DomainsServerTransport.
func (d *DomainsServerTransport) Do(req *http.Request) (*http.Response, error) {
	rawMethod := req.Context().Value(runtime.CtxAPINameKey{})
	method, ok := rawMethod.(string)
	if !ok {
		return nil, nonRetriableError{errors.New("unable to dispatch request, missing value for CtxAPINameKey")}
	}

	parts := strings.Split(method, ".")
	method = parts[1] + "." + parts[2]
	return d.dispatchToMethodFake(req, method)
}

func (d *DomainsServerTransport) dispatchToMethodFake(req *http.Request, method string) (*http.Response, error) {
	resultChan := make(chan result)
	defer close(resultChan)

	go func() {
		var intercepted bool
		var res result
		if domainsServerTransportInterceptor != nil {
			res.resp, res.err, intercepted = domainsServerTransportInterceptor.Do(req)
		}
		if !intercepted {
			switch method {
			case "DomainsClient.GetDomain":
				res.resp, res.err = d.dispatchGetDomain(req)
			case "DomainsClient.NewListDomainsPager":
				res.resp, res.err = d.dispatchNewListDomainsPager(req)
			default:
				res.err = fmt.Errorf("unhandled API %s", method)
			}

		}
		select {
		case resultChan <- res:
		case <-req.Context().Done():
		}
	}()

	select {
	case <-req.Context().Done():
		return nil, req.Context().Err()
	case res := <-resultChan:
		return res.resp, res.err
	}
}

func (d *DomainsServerTransport) dispatchGetDomain(req *http.Request) (*http.Response, error) {
	if d.srv.GetDomain == nil {
		return nil, &nonRetriableError{errors.New("fake for method GetDomain not implemented")}
	}
	const regexStr = `/v1/domains/(?P<domainId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)`
	regex := regexp.MustCompile(regexStr)
	matches := regex.FindStringSubmatch(req.URL.EscapedPath())
	if matches == nil || len(matches) < 1 {
		return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
	}
	domainIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("domainId")])
	if err != nil {
		return nil, err
	}
	respr, errRespr := d.srv.GetDomain(req.Context(), domainIDParam, nil)
	if respErr := server.GetError(errRespr, req); respErr != nil {
		return nil, respErr
	}
	respContent := server.GetResponseContent(respr)
	if !contains([]int{http.StatusOK}, respContent.HTTPStatus) {
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", respContent.HTTPStatus)}
	}
	resp, err := server.MarshalResponseAsJSON(respContent, server.GetResponse(respr).Domain, req)
	if err != nil {
		return nil, err
	}
	return resp, nil
}

func (d *DomainsServerTransport) dispatchNewListDomainsPager(req *http.Request) (*http.Response, error) {
	if d.srv.NewListDomainsPager == nil {
		return nil, &nonRetriableError{errors.New("fake for method NewListDomainsPager not implemented")}
	}
	newListDomainsPager := d.newListDomainsPager.get(req)
	if newListDomainsPager == nil {
		qp := req.URL.Query()
		continuationTokenUnescaped, err := url.QueryUnescape(qp.Get("continuationToken"))
		if err != nil {
			return nil, err
		}
		continuationTokenParam := getOptional(continuationTokenUnescaped)
		var options *core.DomainsClientListDomainsOptions
		if continuationTokenParam != nil {
			options = &core.DomainsClientListDomainsOptions{
				ContinuationToken: continuationTokenParam,
			}
		}
		resp := d.srv.NewListDomainsPager(options)
		newListDomainsPager = &resp
		d.newListDomainsPager.add(req, newListDomainsPager)
		server.PagerResponderInjectNextLinks(newListDomainsPager, req, func(page *core.DomainsClientListDomainsResponse, createLink func() string) {
			page.ContinuationURI = to.Ptr(createLink())
		})
	}
	resp, err := server.PagerResponderNext(newListDomainsPager, req)
	if err != nil {
		return nil, err
	}
	if !contains([]int{http.StatusOK}, resp.StatusCode) {
		d.newListDomainsPager.remove(req)
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", resp.StatusCode)}
	}
	if !server.PagerResponderMore(newListDomainsPager) {
		d.newListDomainsPager.remove(req)
	}
	return resp, nil
}

// set this to conditionally intercept incoming requests to DomainsServerTransport
var domainsServerTransportInterceptor interface {
	// Do returns true if the server transport should use the returned response/error
	Do(*http.Request) (*http.Response, error, bool)
}
