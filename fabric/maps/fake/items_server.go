// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See LICENSE in the project root for license information.
// Code generated by Microsoft (R) AutoRest Code Generator. DO NOT EDIT.
// Changes may cause incorrect behavior and will be lost if the code is regenerated.
// SPDX-License-Identifier: MIT

package fake

import (
	"context"
	"errors"
	"fmt"
	"net/http"
	"net/url"
	"regexp"
	"strconv"
	"strings"

	azfake "github.com/Azure/azure-sdk-for-go/sdk/azcore/fake"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/fake/server"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/runtime"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/to"

	"github.com/microsoft/fabric-sdk-go/fabric/maps"
)

// ItemsServer is a fake server for instances of the maps.ItemsClient type.
type ItemsServer struct {
	// BeginCreateMap is the fake for method ItemsClient.BeginCreateMap
	// HTTP status codes to indicate success: http.StatusOK, http.StatusCreated, http.StatusAccepted
	BeginCreateMap func(ctx context.Context, workspaceID string, createMapRequest maps.CreateMapRequest, options *maps.ItemsClientBeginCreateMapOptions) (resp azfake.PollerResponder[maps.ItemsClientCreateMapResponse], errResp azfake.ErrorResponder)

	// DeleteMap is the fake for method ItemsClient.DeleteMap
	// HTTP status codes to indicate success: http.StatusOK
	DeleteMap func(ctx context.Context, workspaceID string, mapID string, options *maps.ItemsClientDeleteMapOptions) (resp azfake.Responder[maps.ItemsClientDeleteMapResponse], errResp azfake.ErrorResponder)

	// GetMap is the fake for method ItemsClient.GetMap
	// HTTP status codes to indicate success: http.StatusOK
	GetMap func(ctx context.Context, workspaceID string, mapID string, options *maps.ItemsClientGetMapOptions) (resp azfake.Responder[maps.ItemsClientGetMapResponse], errResp azfake.ErrorResponder)

	// BeginGetMapDefinition is the fake for method ItemsClient.BeginGetMapDefinition
	// HTTP status codes to indicate success: http.StatusOK, http.StatusAccepted
	BeginGetMapDefinition func(ctx context.Context, workspaceID string, mapID string, options *maps.ItemsClientBeginGetMapDefinitionOptions) (resp azfake.PollerResponder[maps.ItemsClientGetMapDefinitionResponse], errResp azfake.ErrorResponder)

	// NewListMapsPager is the fake for method ItemsClient.NewListMapsPager
	// HTTP status codes to indicate success: http.StatusOK
	NewListMapsPager func(workspaceID string, options *maps.ItemsClientListMapsOptions) (resp azfake.PagerResponder[maps.ItemsClientListMapsResponse])

	// UpdateMap is the fake for method ItemsClient.UpdateMap
	// HTTP status codes to indicate success: http.StatusOK
	UpdateMap func(ctx context.Context, workspaceID string, mapID string, updateMapRequest maps.UpdateMapRequest, options *maps.ItemsClientUpdateMapOptions) (resp azfake.Responder[maps.ItemsClientUpdateMapResponse], errResp azfake.ErrorResponder)

	// BeginUpdateMapDefinition is the fake for method ItemsClient.BeginUpdateMapDefinition
	// HTTP status codes to indicate success: http.StatusOK, http.StatusAccepted, http.StatusNoContent
	BeginUpdateMapDefinition func(ctx context.Context, workspaceID string, mapID string, updateMapDefinitionRequest maps.UpdateMapDefinitionRequest, options *maps.ItemsClientBeginUpdateMapDefinitionOptions) (resp azfake.PollerResponder[maps.ItemsClientUpdateMapDefinitionResponse], errResp azfake.ErrorResponder)
}

// NewItemsServerTransport creates a new instance of ItemsServerTransport with the provided implementation.
// The returned ItemsServerTransport instance is connected to an instance of maps.ItemsClient via the
// azcore.ClientOptions.Transporter field in the client's constructor parameters.
func NewItemsServerTransport(srv *ItemsServer) *ItemsServerTransport {
	return &ItemsServerTransport{
		srv:                      srv,
		beginCreateMap:           newTracker[azfake.PollerResponder[maps.ItemsClientCreateMapResponse]](),
		beginGetMapDefinition:    newTracker[azfake.PollerResponder[maps.ItemsClientGetMapDefinitionResponse]](),
		newListMapsPager:         newTracker[azfake.PagerResponder[maps.ItemsClientListMapsResponse]](),
		beginUpdateMapDefinition: newTracker[azfake.PollerResponder[maps.ItemsClientUpdateMapDefinitionResponse]](),
	}
}

// ItemsServerTransport connects instances of maps.ItemsClient to instances of ItemsServer.
// Don't use this type directly, use NewItemsServerTransport instead.
type ItemsServerTransport struct {
	srv                      *ItemsServer
	beginCreateMap           *tracker[azfake.PollerResponder[maps.ItemsClientCreateMapResponse]]
	beginGetMapDefinition    *tracker[azfake.PollerResponder[maps.ItemsClientGetMapDefinitionResponse]]
	newListMapsPager         *tracker[azfake.PagerResponder[maps.ItemsClientListMapsResponse]]
	beginUpdateMapDefinition *tracker[azfake.PollerResponder[maps.ItemsClientUpdateMapDefinitionResponse]]
}

// Do implements the policy.Transporter interface for ItemsServerTransport.
func (i *ItemsServerTransport) Do(req *http.Request) (*http.Response, error) {
	rawMethod := req.Context().Value(runtime.CtxAPINameKey{})
	method, ok := rawMethod.(string)
	if !ok {
		return nil, nonRetriableError{errors.New("unable to dispatch request, missing value for CtxAPINameKey")}
	}

	parts := strings.Split(method, ".")
	method = parts[1] + "." + parts[2]
	return i.dispatchToMethodFake(req, method)
}

func (i *ItemsServerTransport) dispatchToMethodFake(req *http.Request, method string) (*http.Response, error) {
	resultChan := make(chan result)
	defer close(resultChan)

	go func() {
		var intercepted bool
		var res result
		if itemsServerTransportInterceptor != nil {
			res.resp, res.err, intercepted = itemsServerTransportInterceptor.Do(req)
		}
		if !intercepted {
			switch method {
			case "ItemsClient.BeginCreateMap":
				res.resp, res.err = i.dispatchBeginCreateMap(req)
			case "ItemsClient.DeleteMap":
				res.resp, res.err = i.dispatchDeleteMap(req)
			case "ItemsClient.GetMap":
				res.resp, res.err = i.dispatchGetMap(req)
			case "ItemsClient.BeginGetMapDefinition":
				res.resp, res.err = i.dispatchBeginGetMapDefinition(req)
			case "ItemsClient.NewListMapsPager":
				res.resp, res.err = i.dispatchNewListMapsPager(req)
			case "ItemsClient.UpdateMap":
				res.resp, res.err = i.dispatchUpdateMap(req)
			case "ItemsClient.BeginUpdateMapDefinition":
				res.resp, res.err = i.dispatchBeginUpdateMapDefinition(req)
			default:
				res.err = fmt.Errorf("unhandled API %s", method)
			}

		}
		select {
		case resultChan <- res:
		case <-req.Context().Done():
		}
	}()

	select {
	case <-req.Context().Done():
		return nil, req.Context().Err()
	case res := <-resultChan:
		return res.resp, res.err
	}
}

func (i *ItemsServerTransport) dispatchBeginCreateMap(req *http.Request) (*http.Response, error) {
	if i.srv.BeginCreateMap == nil {
		return nil, &nonRetriableError{errors.New("fake for method BeginCreateMap not implemented")}
	}
	beginCreateMap := i.beginCreateMap.get(req)
	if beginCreateMap == nil {
		const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/Maps`
		regex := regexp.MustCompile(regexStr)
		matches := regex.FindStringSubmatch(req.URL.EscapedPath())
		if matches == nil || len(matches) < 1 {
			return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
		}
		body, err := server.UnmarshalRequestAsJSON[maps.CreateMapRequest](req)
		if err != nil {
			return nil, err
		}
		workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
		if err != nil {
			return nil, err
		}
		respr, errRespr := i.srv.BeginCreateMap(req.Context(), workspaceIDParam, body, nil)
		if respErr := server.GetError(errRespr, req); respErr != nil {
			return nil, respErr
		}
		beginCreateMap = &respr
		i.beginCreateMap.add(req, beginCreateMap)
	}

	resp, err := server.PollerResponderNext(beginCreateMap, req)
	if err != nil {
		return nil, err
	}

	if !contains([]int{http.StatusOK, http.StatusCreated, http.StatusAccepted}, resp.StatusCode) {
		i.beginCreateMap.remove(req)
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK, http.StatusCreated, http.StatusAccepted", resp.StatusCode)}
	}
	if !server.PollerResponderMore(beginCreateMap) {
		i.beginCreateMap.remove(req)
	}

	return resp, nil
}

func (i *ItemsServerTransport) dispatchDeleteMap(req *http.Request) (*http.Response, error) {
	if i.srv.DeleteMap == nil {
		return nil, &nonRetriableError{errors.New("fake for method DeleteMap not implemented")}
	}
	const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/Maps/(?P<MapId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)`
	regex := regexp.MustCompile(regexStr)
	matches := regex.FindStringSubmatch(req.URL.EscapedPath())
	if matches == nil || len(matches) < 2 {
		return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
	}
	workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
	if err != nil {
		return nil, err
	}
	mapIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("MapId")])
	if err != nil {
		return nil, err
	}
	respr, errRespr := i.srv.DeleteMap(req.Context(), workspaceIDParam, mapIDParam, nil)
	if respErr := server.GetError(errRespr, req); respErr != nil {
		return nil, respErr
	}
	respContent := server.GetResponseContent(respr)
	if !contains([]int{http.StatusOK}, respContent.HTTPStatus) {
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", respContent.HTTPStatus)}
	}
	resp, err := server.NewResponse(respContent, req, nil)
	if err != nil {
		return nil, err
	}
	return resp, nil
}

func (i *ItemsServerTransport) dispatchGetMap(req *http.Request) (*http.Response, error) {
	if i.srv.GetMap == nil {
		return nil, &nonRetriableError{errors.New("fake for method GetMap not implemented")}
	}
	const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/Maps/(?P<MapId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)`
	regex := regexp.MustCompile(regexStr)
	matches := regex.FindStringSubmatch(req.URL.EscapedPath())
	if matches == nil || len(matches) < 2 {
		return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
	}
	workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
	if err != nil {
		return nil, err
	}
	mapIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("MapId")])
	if err != nil {
		return nil, err
	}
	respr, errRespr := i.srv.GetMap(req.Context(), workspaceIDParam, mapIDParam, nil)
	if respErr := server.GetError(errRespr, req); respErr != nil {
		return nil, respErr
	}
	respContent := server.GetResponseContent(respr)
	if !contains([]int{http.StatusOK}, respContent.HTTPStatus) {
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", respContent.HTTPStatus)}
	}
	resp, err := server.MarshalResponseAsJSON(respContent, server.GetResponse(respr).Map, req)
	if err != nil {
		return nil, err
	}
	return resp, nil
}

func (i *ItemsServerTransport) dispatchBeginGetMapDefinition(req *http.Request) (*http.Response, error) {
	if i.srv.BeginGetMapDefinition == nil {
		return nil, &nonRetriableError{errors.New("fake for method BeginGetMapDefinition not implemented")}
	}
	beginGetMapDefinition := i.beginGetMapDefinition.get(req)
	if beginGetMapDefinition == nil {
		const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/Maps/(?P<MapId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/getDefinition`
		regex := regexp.MustCompile(regexStr)
		matches := regex.FindStringSubmatch(req.URL.EscapedPath())
		if matches == nil || len(matches) < 2 {
			return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
		}
		qp := req.URL.Query()
		workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
		if err != nil {
			return nil, err
		}
		mapIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("MapId")])
		if err != nil {
			return nil, err
		}
		formatUnescaped, err := url.QueryUnescape(qp.Get("format"))
		if err != nil {
			return nil, err
		}
		formatParam := getOptional(formatUnescaped)
		var options *maps.ItemsClientBeginGetMapDefinitionOptions
		if formatParam != nil {
			options = &maps.ItemsClientBeginGetMapDefinitionOptions{
				Format: formatParam,
			}
		}
		respr, errRespr := i.srv.BeginGetMapDefinition(req.Context(), workspaceIDParam, mapIDParam, options)
		if respErr := server.GetError(errRespr, req); respErr != nil {
			return nil, respErr
		}
		beginGetMapDefinition = &respr
		i.beginGetMapDefinition.add(req, beginGetMapDefinition)
	}

	resp, err := server.PollerResponderNext(beginGetMapDefinition, req)
	if err != nil {
		return nil, err
	}

	if !contains([]int{http.StatusOK, http.StatusAccepted}, resp.StatusCode) {
		i.beginGetMapDefinition.remove(req)
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK, http.StatusAccepted", resp.StatusCode)}
	}
	if !server.PollerResponderMore(beginGetMapDefinition) {
		i.beginGetMapDefinition.remove(req)
	}

	return resp, nil
}

func (i *ItemsServerTransport) dispatchNewListMapsPager(req *http.Request) (*http.Response, error) {
	if i.srv.NewListMapsPager == nil {
		return nil, &nonRetriableError{errors.New("fake for method NewListMapsPager not implemented")}
	}
	newListMapsPager := i.newListMapsPager.get(req)
	if newListMapsPager == nil {
		const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/Maps`
		regex := regexp.MustCompile(regexStr)
		matches := regex.FindStringSubmatch(req.URL.EscapedPath())
		if matches == nil || len(matches) < 1 {
			return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
		}
		qp := req.URL.Query()
		workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
		if err != nil {
			return nil, err
		}
		continuationTokenUnescaped, err := url.QueryUnescape(qp.Get("continuationToken"))
		if err != nil {
			return nil, err
		}
		continuationTokenParam := getOptional(continuationTokenUnescaped)
		var options *maps.ItemsClientListMapsOptions
		if continuationTokenParam != nil {
			options = &maps.ItemsClientListMapsOptions{
				ContinuationToken: continuationTokenParam,
			}
		}
		resp := i.srv.NewListMapsPager(workspaceIDParam, options)
		newListMapsPager = &resp
		i.newListMapsPager.add(req, newListMapsPager)
		server.PagerResponderInjectNextLinks(newListMapsPager, req, func(page *maps.ItemsClientListMapsResponse, createLink func() string) {
			page.ContinuationURI = to.Ptr(createLink())
		})
	}
	resp, err := server.PagerResponderNext(newListMapsPager, req)
	if err != nil {
		return nil, err
	}
	if !contains([]int{http.StatusOK}, resp.StatusCode) {
		i.newListMapsPager.remove(req)
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", resp.StatusCode)}
	}
	if !server.PagerResponderMore(newListMapsPager) {
		i.newListMapsPager.remove(req)
	}
	return resp, nil
}

func (i *ItemsServerTransport) dispatchUpdateMap(req *http.Request) (*http.Response, error) {
	if i.srv.UpdateMap == nil {
		return nil, &nonRetriableError{errors.New("fake for method UpdateMap not implemented")}
	}
	const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/Maps/(?P<MapId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)`
	regex := regexp.MustCompile(regexStr)
	matches := regex.FindStringSubmatch(req.URL.EscapedPath())
	if matches == nil || len(matches) < 2 {
		return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
	}
	body, err := server.UnmarshalRequestAsJSON[maps.UpdateMapRequest](req)
	if err != nil {
		return nil, err
	}
	workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
	if err != nil {
		return nil, err
	}
	mapIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("MapId")])
	if err != nil {
		return nil, err
	}
	respr, errRespr := i.srv.UpdateMap(req.Context(), workspaceIDParam, mapIDParam, body, nil)
	if respErr := server.GetError(errRespr, req); respErr != nil {
		return nil, respErr
	}
	respContent := server.GetResponseContent(respr)
	if !contains([]int{http.StatusOK}, respContent.HTTPStatus) {
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", respContent.HTTPStatus)}
	}
	resp, err := server.MarshalResponseAsJSON(respContent, server.GetResponse(respr).Map, req)
	if err != nil {
		return nil, err
	}
	return resp, nil
}

func (i *ItemsServerTransport) dispatchBeginUpdateMapDefinition(req *http.Request) (*http.Response, error) {
	if i.srv.BeginUpdateMapDefinition == nil {
		return nil, &nonRetriableError{errors.New("fake for method BeginUpdateMapDefinition not implemented")}
	}
	beginUpdateMapDefinition := i.beginUpdateMapDefinition.get(req)
	if beginUpdateMapDefinition == nil {
		const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/Maps/(?P<MapId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/updateDefinition`
		regex := regexp.MustCompile(regexStr)
		matches := regex.FindStringSubmatch(req.URL.EscapedPath())
		if matches == nil || len(matches) < 2 {
			return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
		}
		qp := req.URL.Query()
		body, err := server.UnmarshalRequestAsJSON[maps.UpdateMapDefinitionRequest](req)
		if err != nil {
			return nil, err
		}
		workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
		if err != nil {
			return nil, err
		}
		mapIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("MapId")])
		if err != nil {
			return nil, err
		}
		updateMetadataUnescaped, err := url.QueryUnescape(qp.Get("updateMetadata"))
		if err != nil {
			return nil, err
		}
		updateMetadataParam, err := parseOptional(updateMetadataUnescaped, strconv.ParseBool)
		if err != nil {
			return nil, err
		}
		var options *maps.ItemsClientBeginUpdateMapDefinitionOptions
		if updateMetadataParam != nil {
			options = &maps.ItemsClientBeginUpdateMapDefinitionOptions{
				UpdateMetadata: updateMetadataParam,
			}
		}
		respr, errRespr := i.srv.BeginUpdateMapDefinition(req.Context(), workspaceIDParam, mapIDParam, body, options)
		if respErr := server.GetError(errRespr, req); respErr != nil {
			return nil, respErr
		}
		beginUpdateMapDefinition = &respr
		i.beginUpdateMapDefinition.add(req, beginUpdateMapDefinition)
	}

	resp, err := server.PollerResponderNext(beginUpdateMapDefinition, req)
	if err != nil {
		return nil, err
	}

	if !contains([]int{http.StatusOK, http.StatusAccepted, http.StatusNoContent}, resp.StatusCode) {
		i.beginUpdateMapDefinition.remove(req)
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK, http.StatusAccepted, http.StatusNoContent", resp.StatusCode)}
	}
	if !server.PollerResponderMore(beginUpdateMapDefinition) {
		i.beginUpdateMapDefinition.remove(req)
	}

	return resp, nil
}

// set this to conditionally intercept incoming requests to ItemsServerTransport
var itemsServerTransportInterceptor interface {
	// Do returns true if the server transport should use the returned response/error
	Do(*http.Request) (*http.Response, error, bool)
}
