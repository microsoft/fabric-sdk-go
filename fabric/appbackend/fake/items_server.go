// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See LICENSE in the project root for license information.
// Code generated by Microsoft (R) AutoRest Code Generator. DO NOT EDIT.
// Changes may cause incorrect behavior and will be lost if the code is regenerated.
// SPDX-License-Identifier: MIT

package fake

import (
	"context"
	"errors"
	"fmt"
	"net/http"
	"net/url"
	"regexp"
	"strconv"
	"strings"

	azfake "github.com/Azure/azure-sdk-for-go/sdk/azcore/fake"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/fake/server"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/runtime"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/to"

	"github.com/microsoft/fabric-sdk-go/fabric/appbackend"
)

// ItemsServer is a fake server for instances of the appbackend.ItemsClient type.
type ItemsServer struct {
	// BeginCreateAppBackend is the fake for method ItemsClient.BeginCreateAppBackend
	// HTTP status codes to indicate success: http.StatusOK, http.StatusCreated, http.StatusAccepted
	BeginCreateAppBackend func(ctx context.Context, workspaceID string, createAppBackendRequest appbackend.CreateAppBackendRequest, options *appbackend.ItemsClientBeginCreateAppBackendOptions) (resp azfake.PollerResponder[appbackend.ItemsClientCreateAppBackendResponse], errResp azfake.ErrorResponder)

	// DeleteAppBackend is the fake for method ItemsClient.DeleteAppBackend
	// HTTP status codes to indicate success: http.StatusOK
	DeleteAppBackend func(ctx context.Context, workspaceID string, appBackendID string, options *appbackend.ItemsClientDeleteAppBackendOptions) (resp azfake.Responder[appbackend.ItemsClientDeleteAppBackendResponse], errResp azfake.ErrorResponder)

	// GetAppBackend is the fake for method ItemsClient.GetAppBackend
	// HTTP status codes to indicate success: http.StatusOK
	GetAppBackend func(ctx context.Context, workspaceID string, appBackendID string, options *appbackend.ItemsClientGetAppBackendOptions) (resp azfake.Responder[appbackend.ItemsClientGetAppBackendResponse], errResp azfake.ErrorResponder)

	// NewListAppBackendsPager is the fake for method ItemsClient.NewListAppBackendsPager
	// HTTP status codes to indicate success: http.StatusOK
	NewListAppBackendsPager func(workspaceID string, options *appbackend.ItemsClientListAppBackendsOptions) (resp azfake.PagerResponder[appbackend.ItemsClientListAppBackendsResponse])

	// UpdateAppBackend is the fake for method ItemsClient.UpdateAppBackend
	// HTTP status codes to indicate success: http.StatusOK
	UpdateAppBackend func(ctx context.Context, workspaceID string, appBackendID string, updateAppBackendRequest appbackend.UpdateAppBackendRequest, options *appbackend.ItemsClientUpdateAppBackendOptions) (resp azfake.Responder[appbackend.ItemsClientUpdateAppBackendResponse], errResp azfake.ErrorResponder)
}

// NewItemsServerTransport creates a new instance of ItemsServerTransport with the provided implementation.
// The returned ItemsServerTransport instance is connected to an instance of appbackend.ItemsClient via the
// azcore.ClientOptions.Transporter field in the client's constructor parameters.
func NewItemsServerTransport(srv *ItemsServer) *ItemsServerTransport {
	return &ItemsServerTransport{
		srv:                     srv,
		beginCreateAppBackend:   newTracker[azfake.PollerResponder[appbackend.ItemsClientCreateAppBackendResponse]](),
		newListAppBackendsPager: newTracker[azfake.PagerResponder[appbackend.ItemsClientListAppBackendsResponse]](),
	}
}

// ItemsServerTransport connects instances of appbackend.ItemsClient to instances of ItemsServer.
// Don't use this type directly, use NewItemsServerTransport instead.
type ItemsServerTransport struct {
	srv                     *ItemsServer
	beginCreateAppBackend   *tracker[azfake.PollerResponder[appbackend.ItemsClientCreateAppBackendResponse]]
	newListAppBackendsPager *tracker[azfake.PagerResponder[appbackend.ItemsClientListAppBackendsResponse]]
}

// Do implements the policy.Transporter interface for ItemsServerTransport.
func (i *ItemsServerTransport) Do(req *http.Request) (*http.Response, error) {
	rawMethod := req.Context().Value(runtime.CtxAPINameKey{})
	method, ok := rawMethod.(string)
	if !ok {
		return nil, nonRetriableError{errors.New("unable to dispatch request, missing value for CtxAPINameKey")}
	}

	parts := strings.Split(method, ".")
	method = parts[1] + "." + parts[2]
	return i.dispatchToMethodFake(req, method)
}

func (i *ItemsServerTransport) dispatchToMethodFake(req *http.Request, method string) (*http.Response, error) {
	resultChan := make(chan result)
	defer close(resultChan)

	go func() {
		var intercepted bool
		var res result
		if itemsServerTransportInterceptor != nil {
			res.resp, res.err, intercepted = itemsServerTransportInterceptor.Do(req)
		}
		if !intercepted {
			switch method {
			case "ItemsClient.BeginCreateAppBackend":
				res.resp, res.err = i.dispatchBeginCreateAppBackend(req)
			case "ItemsClient.DeleteAppBackend":
				res.resp, res.err = i.dispatchDeleteAppBackend(req)
			case "ItemsClient.GetAppBackend":
				res.resp, res.err = i.dispatchGetAppBackend(req)
			case "ItemsClient.NewListAppBackendsPager":
				res.resp, res.err = i.dispatchNewListAppBackendsPager(req)
			case "ItemsClient.UpdateAppBackend":
				res.resp, res.err = i.dispatchUpdateAppBackend(req)
			default:
				res.err = fmt.Errorf("unhandled API %s", method)
			}

		}
		select {
		case resultChan <- res:
		case <-req.Context().Done():
		}
	}()

	select {
	case <-req.Context().Done():
		return nil, req.Context().Err()
	case res := <-resultChan:
		return res.resp, res.err
	}
}

func (i *ItemsServerTransport) dispatchBeginCreateAppBackend(req *http.Request) (*http.Response, error) {
	if i.srv.BeginCreateAppBackend == nil {
		return nil, &nonRetriableError{errors.New("fake for method BeginCreateAppBackend not implemented")}
	}
	beginCreateAppBackend := i.beginCreateAppBackend.get(req)
	if beginCreateAppBackend == nil {
		const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/appBackends`
		regex := regexp.MustCompile(regexStr)
		matches := regex.FindStringSubmatch(req.URL.EscapedPath())
		if len(matches) < 2 {
			return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
		}
		body, err := server.UnmarshalRequestAsJSON[appbackend.CreateAppBackendRequest](req)
		if err != nil {
			return nil, err
		}
		workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
		if err != nil {
			return nil, err
		}
		respr, errRespr := i.srv.BeginCreateAppBackend(req.Context(), workspaceIDParam, body, nil)
		if respErr := server.GetError(errRespr, req); respErr != nil {
			return nil, respErr
		}
		beginCreateAppBackend = &respr
		i.beginCreateAppBackend.add(req, beginCreateAppBackend)
	}

	resp, err := server.PollerResponderNext(beginCreateAppBackend, req)
	if err != nil {
		return nil, err
	}

	if !contains([]int{http.StatusOK, http.StatusCreated, http.StatusAccepted}, resp.StatusCode) {
		i.beginCreateAppBackend.remove(req)
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK, http.StatusCreated, http.StatusAccepted", resp.StatusCode)}
	}
	if !server.PollerResponderMore(beginCreateAppBackend) {
		i.beginCreateAppBackend.remove(req)
	}

	return resp, nil
}

func (i *ItemsServerTransport) dispatchDeleteAppBackend(req *http.Request) (*http.Response, error) {
	if i.srv.DeleteAppBackend == nil {
		return nil, &nonRetriableError{errors.New("fake for method DeleteAppBackend not implemented")}
	}
	const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/appBackends/(?P<appBackendId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)`
	regex := regexp.MustCompile(regexStr)
	matches := regex.FindStringSubmatch(req.URL.EscapedPath())
	if len(matches) < 3 {
		return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
	}
	workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
	if err != nil {
		return nil, err
	}
	appBackendIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("appBackendId")])
	if err != nil {
		return nil, err
	}
	respr, errRespr := i.srv.DeleteAppBackend(req.Context(), workspaceIDParam, appBackendIDParam, nil)
	if respErr := server.GetError(errRespr, req); respErr != nil {
		return nil, respErr
	}
	respContent := server.GetResponseContent(respr)
	if !contains([]int{http.StatusOK}, respContent.HTTPStatus) {
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", respContent.HTTPStatus)}
	}
	resp, err := server.NewResponse(respContent, req, nil)
	if err != nil {
		return nil, err
	}
	return resp, nil
}

func (i *ItemsServerTransport) dispatchGetAppBackend(req *http.Request) (*http.Response, error) {
	if i.srv.GetAppBackend == nil {
		return nil, &nonRetriableError{errors.New("fake for method GetAppBackend not implemented")}
	}
	const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/appBackends/(?P<appBackendId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)`
	regex := regexp.MustCompile(regexStr)
	matches := regex.FindStringSubmatch(req.URL.EscapedPath())
	if len(matches) < 3 {
		return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
	}
	workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
	if err != nil {
		return nil, err
	}
	appBackendIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("appBackendId")])
	if err != nil {
		return nil, err
	}
	respr, errRespr := i.srv.GetAppBackend(req.Context(), workspaceIDParam, appBackendIDParam, nil)
	if respErr := server.GetError(errRespr, req); respErr != nil {
		return nil, respErr
	}
	respContent := server.GetResponseContent(respr)
	if !contains([]int{http.StatusOK}, respContent.HTTPStatus) {
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", respContent.HTTPStatus)}
	}
	resp, err := server.MarshalResponseAsJSON(respContent, server.GetResponse(respr).AppBackend, req)
	if err != nil {
		return nil, err
	}
	return resp, nil
}

func (i *ItemsServerTransport) dispatchNewListAppBackendsPager(req *http.Request) (*http.Response, error) {
	if i.srv.NewListAppBackendsPager == nil {
		return nil, &nonRetriableError{errors.New("fake for method NewListAppBackendsPager not implemented")}
	}
	newListAppBackendsPager := i.newListAppBackendsPager.get(req)
	if newListAppBackendsPager == nil {
		const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/appBackends`
		regex := regexp.MustCompile(regexStr)
		matches := regex.FindStringSubmatch(req.URL.EscapedPath())
		if len(matches) < 2 {
			return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
		}
		qp := req.URL.Query()
		workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
		if err != nil {
			return nil, err
		}
		recursiveUnescaped, err := url.QueryUnescape(qp.Get("recursive"))
		if err != nil {
			return nil, err
		}
		recursiveParam, err := parseOptional(recursiveUnescaped, strconv.ParseBool)
		if err != nil {
			return nil, err
		}
		rootFolderIDUnescaped, err := url.QueryUnescape(qp.Get("rootFolderId"))
		if err != nil {
			return nil, err
		}
		rootFolderIDParam := getOptional(rootFolderIDUnescaped)
		continuationTokenUnescaped, err := url.QueryUnescape(qp.Get("continuationToken"))
		if err != nil {
			return nil, err
		}
		continuationTokenParam := getOptional(continuationTokenUnescaped)
		var options *appbackend.ItemsClientListAppBackendsOptions
		if recursiveParam != nil || rootFolderIDParam != nil || continuationTokenParam != nil {
			options = &appbackend.ItemsClientListAppBackendsOptions{
				Recursive:         recursiveParam,
				RootFolderID:      rootFolderIDParam,
				ContinuationToken: continuationTokenParam,
			}
		}
		resp := i.srv.NewListAppBackendsPager(workspaceIDParam, options)
		newListAppBackendsPager = &resp
		i.newListAppBackendsPager.add(req, newListAppBackendsPager)
		server.PagerResponderInjectNextLinks(newListAppBackendsPager, req, func(page *appbackend.ItemsClientListAppBackendsResponse, createLink func() string) {
			page.ContinuationURI = to.Ptr(createLink())
		})
	}
	resp, err := server.PagerResponderNext(newListAppBackendsPager, req)
	if err != nil {
		return nil, err
	}
	if !contains([]int{http.StatusOK}, resp.StatusCode) {
		i.newListAppBackendsPager.remove(req)
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", resp.StatusCode)}
	}
	if !server.PagerResponderMore(newListAppBackendsPager) {
		i.newListAppBackendsPager.remove(req)
	}
	return resp, nil
}

func (i *ItemsServerTransport) dispatchUpdateAppBackend(req *http.Request) (*http.Response, error) {
	if i.srv.UpdateAppBackend == nil {
		return nil, &nonRetriableError{errors.New("fake for method UpdateAppBackend not implemented")}
	}
	const regexStr = `/v1/workspaces/(?P<workspaceId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)/appBackends/(?P<appBackendId>[!#&$-;=?-\[\]_a-zA-Z0-9~%@]+)`
	regex := regexp.MustCompile(regexStr)
	matches := regex.FindStringSubmatch(req.URL.EscapedPath())
	if len(matches) < 3 {
		return nil, fmt.Errorf("failed to parse path %s", req.URL.Path)
	}
	body, err := server.UnmarshalRequestAsJSON[appbackend.UpdateAppBackendRequest](req)
	if err != nil {
		return nil, err
	}
	workspaceIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("workspaceId")])
	if err != nil {
		return nil, err
	}
	appBackendIDParam, err := url.PathUnescape(matches[regex.SubexpIndex("appBackendId")])
	if err != nil {
		return nil, err
	}
	respr, errRespr := i.srv.UpdateAppBackend(req.Context(), workspaceIDParam, appBackendIDParam, body, nil)
	if respErr := server.GetError(errRespr, req); respErr != nil {
		return nil, respErr
	}
	respContent := server.GetResponseContent(respr)
	if !contains([]int{http.StatusOK}, respContent.HTTPStatus) {
		return nil, &nonRetriableError{fmt.Errorf("unexpected status code %d. acceptable values are http.StatusOK", respContent.HTTPStatus)}
	}
	resp, err := server.MarshalResponseAsJSON(respContent, server.GetResponse(respr).AppBackend, req)
	if err != nil {
		return nil, err
	}
	return resp, nil
}

// set this to conditionally intercept incoming requests to ItemsServerTransport
var itemsServerTransportInterceptor interface {
	// Do returns true if the server transport should use the returned response/error
	Do(*http.Request) (*http.Response, error, bool)
}
